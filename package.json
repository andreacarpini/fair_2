{
  "name": "chicago-movers-tailwind",
  "version": "1.0.0",
  "description": "Chicago Pro Movers website with Tailwind CSS",
  "scripts": {
    "build-css": "tailwindcss -i ./src/input.css -o ./assets/css/style.css --watch",
    "build": "bundle exec jekyll build",
    "serve": "bundle exec jekyll serve"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
